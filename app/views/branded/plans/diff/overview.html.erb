<%# locals: { plan } %>

<%= render partial: 'shared/header', locals: { plan: @plan, title_suffix: _('Overview') } %>

<div class="row">
  <div class="col-md-12">
    <%= render partial: 'overview_details', layout: 'navigation', locals: { plan: plan } %>
  </div>
</div>
