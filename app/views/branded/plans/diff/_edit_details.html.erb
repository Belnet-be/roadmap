<%# locals: plan %>

<%= form_for plan, html: { method: :put,
                            class: 'form-horizontal edit_plan' } do |f| %>
  <div class="row">
    <div class="col-lg-8">
      <%= render partial: "plans/project_details",
                 locals: { form: f, plan: plan, orgs: orgs, org_partial: org_partial,
                           research_domains: research_domains } %>

      <%= f.button(_('Save'), class: "btn btn-secondary", type: "submit") %>
    </div>
    <div class="col-lg-4">
      <h2><%= _('Select Guidance') %></h2>

      <%= render partial: "guidance_selection",
                 locals: {
                   form: f,
                   all_guidance_groups: all_guidance_groups,
                   important_ggs: important_ggs,
                   selected_guidance_groups: selected_guidance_groups,
                   all_ggs_grouped_by_org: all_ggs_grouped_by_org
                 } %>
    </div>
  </div>
<% end %>
