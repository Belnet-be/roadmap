<% phases = plan.template.phases %>
<ul class="nav nav-tabs" role="tablist">
  <li role="presentation" class="nav-item <%= (active_page?(@navigation_button_show) ? 'active' : '') %>">
    <a href="<%= @navigation_button_show %>" role="tab" 
    class="nav-link" aria-controls="content"><%= _('Project Details') %></a>
  </li>
  <li role="presentation" class="nav-item <%= (active_page?(@navigation_button_overview) ? 'active' : '') %>">
    <%= link_to _('Plan overview'), @navigation_button_overview, role: 'tab', 
                aria: { controls: "content" }, class: 'nav-link' %>
  <% phases.each do |phase| %>
    <li role="presentation" class="nav-item <%= (active_page?(diff_edit_plan_path(base_plan: supposed_base_plan, head_plan: supposed_head_plan, phase_id: phase.id)) ? 'active' : '') %>">
      <a href="<%= diff_edit_plan_path(base_plan: supposed_base_plan, head_plan: supposed_head_plan, phase_id: phase.id) %>" role="tab" 
         class="nav-link" aria-controls="content">
        <%= (phases.size > 1 ? phase.title : _('Write Plan')) %>
      </a>
    </li>
  <% end %>
</ul>
<div class="tab-content">
  <div id="content" role="tabpanel" class="tab-pane active">
    <div class="card card-default">
      <div class="card-body">
        <%= yield :plan_tab_body %>
      </div>
    </div>
  </div>
</div>
