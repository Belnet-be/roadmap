<%= render partial: 'shared/header', locals: { plan: @plan, title_suffix: _('Details') } %>

<div class="row">
  <div class="col-md-12">
    <%= render partial: @partial, layout: 'navigation',
               locals: { plan: @plan, visibility: @visibility, orgs: @orgs, org_partial: @org_partial } %>
  </div>
</div>
