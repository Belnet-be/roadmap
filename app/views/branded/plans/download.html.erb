<%= render partial: 'header', locals: { plan: @plan, title_suffix: _('Download') } %>

<div class="row">
  <div class="col-md-12">
    <%= render partial: 'download_form', layout: 'navigation', locals: { plan: @plan } %>
  </div>
</div>
