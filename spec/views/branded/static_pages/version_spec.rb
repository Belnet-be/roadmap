# frozen_string_literal: true

require 'rails_helper'

RSpec.describe 'branded/static_pages/version', type: :view do
  # Add tests that check if the page loads with and without ENV variables later!!
end
