web: unset PORT && bin/rails server
js: if [ "$BELNET_SPLITTED_LOGS" = "true" ]; then yarn build --watch 2>&1 | tee -a log/js.log; else yarn build --watch; fi
css: if [ "$BELNET_SPLITTED_LOGS" = "true" ]; then yarn build:css --watch 2>&1 | tee -a log/css.log; else yarn build:css --watch; fi
